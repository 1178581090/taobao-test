# 开发日志

## 2026-05-26

### 项目初始化

- 创建 `E:\taobaoTest` 项目目录
- 初始化 Git 仓库
- 生成 CLAUDE.md 项目文档

### 标题诊断工具 v1

- 创建 `index.html`，纯前端单页面，浏览器直接打开
- 三个功能标签页：
  - **标题诊断** — 8维度评分（长度、产品词、属性词、场景词、定制词、重复词、特殊符号、人群词），满分100
  - **标题生成** — 按字段填写自动拼接标题
  - **优化技巧** — 演出服类目标题公式和规则速查
- 修复 emoji 正则语法错误（JS 不支持 `[😀-🙏]` 范围）
- 修复评分上限（105 → 封顶100）

### 工作流验证

- 确认可通过保存淘宝页面 HTML 文件分析店铺内容
- 确认可通过截图文件路径分析店铺页面

### 待定

- 后续功能需求待用户确认：诊断维度、数据来源方式、目标用户
- 下次新会话可直接开工，CLAUDE.md 已就位

## 2026-05-27

### 浏览器扩展验证与适配

- 在真实淘宝搜索结果页完成扩展端到端测试
- 发现旧选择器全部失效，淘宝已改为 CSS Modules 动态类名
- 4 轮 DOM 诊断确定新选择器映射：
  - 商品卡片 → `a[class*="doubleCardWrapperAdapt"]`
  - 标题 → `[class*="title--"]`
  - 价格整数/小数 → `[class*="priceInt--"]` / `[class*="priceFloat--"]`
  - 店铺 → `[class*="shopName--"]`
  - 天猫判断 → 链接 href 含 `detail.tmall.com`
  - 活动标签 → `[class*="subIconWrapper--"]` > span
- 重写 `content.js` 的 `extractSingleProduct()` 全部选择器

### 销量数据提取修复

- 淘宝将价格（如 ¥20.8）和销量（如 3000+人付款）拼接在同一个文本节点：`¥20.83000+人付款`
- 旧版正则 `([\d.]+万?\+?人付款)` 贪婪匹配导致价格小数位数字被吞入销量（83000 而非 3000）
- 修复方案：用 `¥` + `priceInt` 定位价格在文本中的起始位置，跳过 `.` + `priceFloat` 后取剩余文本，再匹配销量模式

### 端到端验证

- 扩展在淘宝搜索页成功提取 42 个商品（标题/价格/销量/店铺/活动标签均正确）
- 扩展 → 剪贴板 → index.html 竞品分析 → 明日清单 完整链路跑通
