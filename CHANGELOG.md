# 开发日志

## 2026-05-28

- 用商品ID精确匹配自己商品，售价统一从扩展抓取

## 2026-05-26

### 项目初始化

- 创建 `E:\taobaoTest` 项目目录
- 初始化 Git 仓库
- 生成 CLAUDE.md 项目文档

### 标题诊断工具 v1

- 创建 `index.html`，纯前端单页面，浏览器直接打开
- 三个功能标签页：
  - **标题诊断** — 8维度评分（长度、产品词、属性词、场景词、定制词、重复词、特殊符号、人群词），满分100
  - **标题生成** — 按字段填写自动拼接标题
  - **优化技巧** — 演出服类目标题公式和规则速查
- 修复 emoji 正则语法错误（JS 不支持 `[😀-🙏]` 范围）
- 修复评分上限（105 → 封顶100）

### 工作流验证

- 确认可通过保存淘宝页面 HTML 文件分析店铺内容
- 确认可通过截图文件路径分析店铺页面

### 待定

- 后续功能需求待用户确认：诊断维度、数据来源方式、目标用户
- 下次新会话可直接开工，CLAUDE.md 已就位

## 2026-05-27
- 完善阶段收尾脚本，自动更新开发日志

### 浏览器扩展验证与适配

- 在真实淘宝搜索结果页完成扩展端到端测试
- 发现旧选择器全部失效，淘宝已改为 CSS Modules 动态类名
- 4 轮 DOM 诊断确定新选择器映射：
  - 商品卡片 → `a[class*="doubleCardWrapperAdapt"]`
  - 标题 → `[class*="title--"]`
  - 价格整数/小数 → `[class*="priceInt--"]` / `[class*="priceFloat--"]`
  - 店铺 → `[class*="shopName--"]`
  - 天猫判断 → 链接 href 含 `detail.tmall.com`
  - 活动标签 → `[class*="subIconWrapper--"]` > span
- 重写 `content.js` 的 `extractSingleProduct()` 全部选择器

### 销量数据提取修复

- 淘宝将价格（如 ¥20.8）和销量（如 3000+人付款）拼接在同一个文本节点：`¥20.83000+人付款`
- 旧版正则 `([\d.]+万?\+?人付款)` 贪婪匹配导致价格小数位数字被吞入销量（83000 而非 3000）
- 修复方案：用 `¥` + `priceInt` 定位价格在文本中的起始位置，跳过 `.` + `priceFloat` 后取剩余文本，再匹配销量模式

### 端到端验证

- 扩展在淘宝搜索页成功提取 42 个商品（标题/价格/销量/店铺/活动标签均正确）
- 扩展 → 剪贴板 → index.html 竞品分析 → 明日清单 完整链路跑通

### 扩展：商品详情页支持

- `content.js` 新增页面类型检测（search / product / unknown）
- 新增 `getProductTitle()` — `document.title` 正则解析（"标题-淘宝网"/"标题-天猫"格式）+ heading 元素回退
- 新增 `extractKeywords(title)` — 三步提取：分隔符切分 → 领域词典匹配（100+ 演出服/红楼梦/材质/场景相关词）→ n-gram 填补空白区
- 新增 `showProductPageBar(title, keywords)` — 页顶交互栏，关键词可点击移除/恢复，支持手动添加，一键跳转搜索
- `manifest.json` 扩展 `host_permissions` 和 `content_scripts.matches` 至 item/detail 页面
- 词典覆盖：品类词、场景词、定制词、材质工艺、红楼梦人物、古装角色、属性风格词

### 竞品分析：行动方案卡片

- 四张分析卡片上方新增「行动方案」综合卡片（`renderActionPlan()`），按优先级输出 3-5 条可执行操作
- 优先级 1 — 标题优化：同行高频词标签 + 建议标题 + 缺失词提示 + 复制/去诊断按钮
- 优先级 2 — 定价建议（价格偏离 > 15% 时显示）：同行均价/分位数 + 具体建议价位 + 降/涨价两种策略
- 优先级 3 — 活动配置（同行 >= 30% 使用）：列出活动名和占比 + 千牛操作路径
- 优先级 4 — 竞品参考：Top5 销量商品列表，含店铺名、价格、销量，标记自有商品

### 关键词提取和标题生成重写

- 关键词碎片问题（"楼梦""出服""装演"）：原 `getKeywordFrequency()` 用全量滑动窗口 n-gram 产生大量碎片
- 重写为词典优先匹配 + 空位 n-gram 填补，同 content.js 的 `extractKeywords()` 逻辑一致
- 标题生成 `generateSuggestedTitle()` 改为连续中文拼接（无空格），不再是词袋堆砌
- 新增碎片过滤：若短词被长词完全包含则剔除（如"红楼"被"红楼梦"包住）
- 词分类改用模式数组，与领域词典保持同步，覆盖品类/风格/材质/场景/人群五类

### 词典动态管理

- 词典存储从硬编码常量改为 `localStorage.tb_domain_dict`，默认词典作为兜底
- `loadDomainDict()` / `saveDomainDict()` 读写，`getDomainDictSorted()` 返回排序后词表
- 竞品分析 Tab 底部新增「词典管理」折叠面板：查看全部词（点击删除）、输入框添加、恢复默认
- 行动方案卡片显示「新词发现」区域：同行 >= 30% 出现但词典未收录的词，点击一键加入词典
- 词典越完整关键词提取越精准，后续品类变化无需改代码

### 阶段收尾脚本

- 创建 `finish-phase.sh`：清理临时文件（`.superpowers/`、`docs/superpowers/`）→ 展示 git 变更 → 提交 → 推送
- 不带参数时仅清理和预览，带提交信息参数时执行完整提交流程
- `.gitignore` 补充 `.claude/` 和 `测试图片/` 排除规则
