# 淘宝店铺诊断工具 - 产品设计文档

## 一、项目概述

### 1.1 项目定位

个人专用的淘宝店铺诊断工具，集成标题优化和竞品分析能力。聚焦**演出服/定制类目**，通过分析淘宝公开搜索结果中的真实数据，给出可执行的操作建议。

### 1.2 目标用户

- 自己使用，不需要多用户、注册登录等功能
- 演出服/古装/舞蹈服/定制类目的淘宝卖家
- 店铺有 0 销量商品、需要破零和新店起量的场景

### 1.3 核心价值

- 标题诊断：输入标题 → 8 维度评分，即时反馈问题
- 竞品对比：基于淘宝真实搜索结果，分析同品类头部商品的标题、价格、销量
- 可执行建议：不只看分数，直接告诉你"改什么、怎么改"

### 1.4 与现有标题工具的关系

现有 `index.html` 已实现的三个功能作为本系统的基础模块保留：

| 模块 | 状态 | 说明 |
|------|------|------|
| 标题诊断 | 已实现 | 8 维度评分（长度、产品词、属性词、场景词、定制词、重复词、符号、人群词），满分 100 |
| 标题生成 | 已实现 | 按"款式 + 材质 + 核心词 + 场景 + 人群 + 定制"公式拼接 |
| 优化技巧 | 已实现 | 静态展示标题公式和基础规则 |

本设计文档在此之上扩展**竞品分析、单品诊断、明日清单**三个新模块。

---

## 二、功能模块

### 2.1 模块总览

| 模块 | 优先级 | 说明 |
|------|--------|------|
| 标题诊断 | 已完成 | 输入标题文本，8 维度评分 |
| 标题生成 | 已完成 | 填写属性字段，按公式拼接标题 |
| 优化技巧 | 已完成 | 静态知识展示 |
| 数据抓取 | P0 | 从淘宝搜索结果页获取公开数据 |
| 竞品分析 | P0 | 基于真实搜索结果对比标题/价格/销量 |
| 单品诊断 | P1 | 输入商品链接，分析标题+价格+主图 |
| 明日清单 | P0 | 根据诊断结果生成可勾选的任务列表 |

### 2.2 数据抓取（基础模块）

**数据来源**：淘宝公开的搜索结果页（`s.taobao.com`），不需要登录即可访问。

**获取方式**（按优先级）：
1. 用户粘贴搜索关键词 → 工具通过 CORS 代理请求搜索结果页 → 解析 HTML 提取商品数据
2. 备选：用户直接在淘宝搜索页复制 HTML 源码粘贴到工具中解析
3. 备选：用户手动输入竞品数据（标题、价格、销量），工具做对比分析

**可提取的公开字段**：
- 商品标题、价格、付款人数（月销）、评价数
- 店铺名称、店铺所在地
- 主图 URL（可判断是否为视频封面）

**不可获取的数据**（淘宝不公开或需登录）：
- 店铺后台数据（访客、转化率、收藏加购等）
- 竞品店铺的详细经营数据
- 店铺层级（非公开指标）

### 2.3 竞品分析

**输入**：用户输入核心关键词（如"古装演出服"），工具抓取搜索结果第一页（约 48 个商品）。

**输出**：

| 分析维度 | 数据来源 | 产出 |
|----------|----------|------|
| 标题关键词 | 搜索结果中高销量商品的标题 | 高频词统计、你的标题缺失了哪些词 |
| 价格分布 | 搜索结果价格区间 | 你的价格在同行中的位置 |
| 销量标杆 | 头部商品月销 | 同品类天花板在哪 |
| 主图/视频 | 搜索结果中视频标记 | 头部商品有多少用了视频 |

**对比规则**：
- 你的价格 > 同行均价 30% → 提示突出定制差异或增加入门款
- 你的标题缺少同行高频词 → 列出缺失词建议补充
- 同行 50%+ 商品有视频而你没有 → 建议加主图视频

### 2.4 单品诊断

**输入**：单个商品的淘宝链接（或手动填写标题+价格）。

**诊断项**：

| 检测项 | 判断逻辑 | 建议 |
|--------|----------|------|
| 标题质量 | 复用标题诊断 8 维度 | 逐项指出扣分原因 |
| 价格合理度 | 与同关键词搜索结果均价对比 | 偏高/偏低/合理 |
| 主图视频 | 商品页是否有视频标记 | 无视频则建议添加 |
| 关键词覆盖 | 标题关键词与搜索热词对比 | 列出未覆盖的高频词 |

### 2.5 明日清单

基于诊断结果自动生成可勾选的任务列表，用 `localStorage` 持久化勾选状态。

**示例任务**：
- [ ] 修改主推款标题，加入"舞台演出""定制"关键词
- [ ] 拍摄 30 秒产品展示视频，上传到主图第 2 位
- [ ] 联系 3 个老客户带图评价
- [ ] 在详情页增加定制流程说明
- [ ] 设置评价有礼活动

清单底部显示完成进度：`✅ 已完成 X / Y 项`。

---

## 三、诊断规则汇总

### 3.1 标题诊断规则（已实现）

| # | 检测项 | 满分 | 判断逻辑 |
|---|--------|------|----------|
| 1 | 标题长度 | 20 | 20-40 字满分，15-20 偏短，40-50 偏长，>50 太长 |
| 2 | 核心产品词 | 20 | 包含"演出服/舞蹈服/古装/盔甲/服装/服饰" |
| 3 | 属性词丰富度 | 20 | ≥2 个属性词满分，1 个半分，0 个零分 |
| 4 | 场景/用途词 | 15 | 包含"舞台/表演/比赛/影楼/摄影/年会"等 |
| 5 | 定制标识 | 10 | 包含"定制/来图/来样/批量" |
| 6 | 无重复词 | 10 | 按空格/逗号分词后检查重复 |
| 7 | 无违规符号 | 5 | 无 ★▼▲ 等特殊符号和连续标点 |
| 8 | 人群/性别词 | 5 | 包含"成人/儿童/男/女/幼儿/少儿" |

总分 = min(各项得分之和, 100)。≥80 优秀，60-79 中等，<60 需改进。

### 3.2 竞品分析规则（待实现）

| 检测项 | 判断逻辑 | 建议输出 |
|--------|----------|----------|
| 关键词缺失 | 标题缺少同行 Top10 商品中出现的 3 个以上高频词 | 列出建议补充的关键词 |
| 价格偏离 | 价格超出同行均价 ±30% | 偏高建议增加入门款或强化定制价值；偏低建议检查利润空间 |
| 主图视频 | 同行 50%+ 有视频标记而本店无 | 建议拍摄 30 秒展示视频 |
| 标题长度 | 同行平均长度 vs 你的标题长度 | 参照同行调整 |

### 3.3 关键词库

**正向词库**（GOOD_KEYWORDS）：演出服、舞蹈服、古装、盔甲、定制、舞台、成人、儿童、影楼、摄影、表演、比赛、戏曲

**属性词库**（用于丰富度检测）：古装、舞蹈、儿童、成人、男女、刺绣、旗袍、汉服、戏曲、舞台剧、古典、民族、现代、芭蕾、拉丁、爵士、街舞、合唱、礼服、婚纱、Cosplay、铠甲、战袍

**场景词库**：舞台、表演、比赛、演出、影楼、摄影、年会、六一、艺考、培训、排练、走秀、庆典

**违规模式**（BAD_PATTERNS）：特殊符号、连续标点、连续感叹号

---

## 四、页面设计规范

### 4.1 整体风格

- 风格：简洁工具风，不需要花哨
- 主色调：淘宝橙 `#ff5000`，与现有标题工具保持一致
- 背景：`#f5f5f5`
- 字体：系统默认无衬线字体

### 4.2 布局

- PC 端最大宽度 `900px`，居中显示（比现有 720px 略宽以容纳新模块）
- Tab 切换模式，不使用多页面跳转
- 移动端自适应，无横向滚动

### 4.3 组件规范

- 按钮：主按钮橙底白字 `border-radius: 8px`；次按钮灰底灰字
- 卡片：白底，`border-radius: 10px`，`padding: 24px`，`box-shadow: 0 1px 4px rgba(0,0,0,.06)`
- 输入框：`border: 1.5px solid #e0e0e0`，focus 时橙色边框
- 评分圆环：≥80 绿色，50-79 橙色，<50 红色

---

## 五、用户操作流程

```
打开工具
  ├─ 标题诊断 Tab：粘贴标题 → 点击诊断 → 查看 8 维度评分和建议
  ├─ 标题生成 Tab：填写属性 → 点击生成 → 复制生成的标题
  ├─ 竞品分析 Tab：输入关键词 → 抓取搜索结果 → 查看对比数据和建议
  ├─ 明日清单 Tab：查看自动生成的任务 → 勾选已完成 → 次日更新
  └─ 优化技巧 Tab：查看静态知识
```

无页面跳转，所有功能通过 Tab 切换在同一页面完成。

---

## 六、技术实现

### 6.1 技术选型

| 项 | 选择 | 原因 |
|----|------|------|
| 前端框架 | 纯 HTML/CSS/JS | 无构建依赖，浏览器直接打开 |
| 数据抓取 | CORS 代理 + DOMParser | 从淘宝搜索页 HTML 提取商品列表 |
| 数据存储 | localStorage | 持久化明日清单勾选和用户偏好 |
| 导出功能 | html2canvas（可选） | P2 低优先级 |

### 6.2 数据抓取流程

```
用户输入关键词（如"古装演出服 定制"）
  → 拼接搜索 URL: https://s.taobao.com/search?q=古装演出服+定制
  → 通过 CORS 代理 GET 请求
  → 返回 HTML → DOMParser 解析
  → 提取商品卡片数据: m-itemlist 中的商品项
  → 结构化输出: [{title, price, sales, shop, hasVideo, ...}]
```

### 6.3 CORS 代理备选方案

由于 CORS 代理可能不稳定，提供降级方案：
1. 用户自己在浏览器打开淘宝搜索页 → 右键查看源代码 → 粘贴到工具的文本框中
2. 工具从粘贴的 HTML 中解析出商品数据
3. 此方案无网络限制，永远可用

### 6.4 文件结构

```
taobaoTest/
  index.html      # 主页面（所有功能在此文件中）
  CLAUDE.md       # 开发说明
  CHANGELOG.md    # 变更日志
  设计文档.md      # 本文档
```

---

## 七、验收标准

| 验收项 | 要求 |
|--------|------|
| 标题诊断 | 输入标题后即时出分，8 项检测逐项显示 pass/fail/tip |
| 标题生成 | 填写属性后生成标题，拼接顺序正确 |
| 竞品分析 | 输入关键词后可获取并展示搜索结果数据 |
| 数据解析 | 从淘宝搜索页 HTML 中正确提取标题/价格/销量 |
| 降级方案 | 粘贴 HTML 源码的方式也能正常解析 |
| 明日清单 | 勾选状态刷新后保持（localStorage），进度条准确 |
| 响应式 | 手机端布局正常，无横向滚动 |
| 代码质量 | 无控制台报错，JS 中无 emoji 正则 |
| 离线可用 | file:// 协议下所有功能正常（除网络抓取外） |

---

## 八、待定 / 不做

| 项目 | 决策 | 原因 |
|------|------|------|
| 淘宝账号授权登录 | 不做 | 单人使用，不需要 |
| 多用户/注册系统 | 不做 | 同上 |
| 店铺层级数据 | 不做 | 淘宝不公开此数据 |
| 后端服务 | 不做 | 保持纯前端，降低维护成本 |
| 报告导出为 PDF | P2 低优 | 当前截图即可满足需求 |
| mock 数据 | 删除 | 改用真实数据 |
| 蓝色配色方案 | 废弃 | 统一使用淘宝橙 |
